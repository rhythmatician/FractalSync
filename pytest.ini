[pytest]
# Ensure tests under scripts/ are discovered when running `pytest` from repo root
testpaths =
    backend
    scripts\build_distance_field

# Keep default test file pattern
python_files = test_*.py

# Recommend verbose output in CI; keep local runs quiet by default
addopts = -q
