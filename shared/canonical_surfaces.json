{
    "$schema": "Cross-surface contract manifest (parity-of-production-paths, post-#93 incident)",
    "rule": "Every canonical shared subsystem listed here must be REACHABLE from each declared required surface through its real public seam. A subsystem that exists on one required surface but not another is an INVALID repository state: CI fails. Reachability is verified by the named required_tests, which must enter through the production entry points (not bypass helpers).",
    "canonical_pipeline_boundary": {
        "rule": "The canonical shared pipeline BEGINS at decoded PCM + native sample rate. Media DECODING is surface-specific and legitimately NOT shared: Python may use librosa/soundfile, the browser may use Web Audio (decodeAudioData / MediaStream). What must be identical is everything downstream: native-rate PCM enters runtime-core (AnalysisTimebase) and the Rust StreamingResampler owns ALL rate conversion on every surface. Never pre-resample in Python (librosa sr=SAMPLE_RATE) or in TypeScript before the timebase — that reintroduces a second resampler authority.",
        "phase_critical_note": "For phase-critical file-mode comparisons, use PCM/WAV fixtures: MP3/AAC decoders apply codec-specific priming/delay that shifts sample alignment between decoders. Live microphone input is already PCM, so the issue does not exist there.",
        "ingestion_contract": "decoded PCM (native rate) + native sample rate -> runtime-core AnalysisTimebase.ingest(samples, source_sample_rate, source_start_frame)"
    },
    "subsystems": {
        "audio_analysis": {
            "authority": "runtime-core/src/timebase.rs (AnalysisTimebase, StreamingResampler)",
            "description": "Canonical 48 kHz sample-clock timebase: exactly-once PCM ingestion, stateful resampling, 1024-sample hop scheduling, epoch semantics, feature extraction.",
            "surfaces": {
                "rust_authority": "runtime-core/src/timebase.rs",
                "runtime_surface": "wasm-orbit/src/lib.rs (AnalysisTimebase wasm_bindgen) consumed by frontend/src/lib/analysisTimebase.ts",
                "training_surface": "runtime-core/src/pybindings.rs (AnalysisTimebase PyO3) consumed by backend/src/data_loader.py"
            },
            "required_tests": [
                "backend/tests/test_audio_pipeline_parity.py",
                "runtime-core/tests/test_timebase.rs"
            ]
        },
        "physics_step": {
            "authority": "runtime-core/src/controller.rs (OrbitController::step, PlayerState::step)",
            "description": "c-space physics integration. The timestep must come from the canonical tick contract (AnalysisTick.dt_seconds = HOP_LENGTH / SAMPLE_RATE), never restated as a literal.",
            "surfaces": {
                "rust_authority": "runtime-core/src/controller.rs",
                "runtime_surface": "wasm-orbit/src/lib.rs (OrbitController) consuming AnalysisTick.dt_seconds from frontend/src/lib/modelInference.ts",
                "training_surface": "backend/src/cspace_proxies.py (differentiable mirrors) consuming dt from the canonical tick contract"
            },
            "required_tests": [
                "backend/tests/test_controller_pipeline_parity.py",
                "backend/tests/test_golden_parity.py"
            ]
        },
        "cycle_bank": {
            "authority": "runtime-core/src/cycle_bank.rs (CycleBank, CycleMode, CycleObservation, CycleRelation) with the canonical seam runtime-core/src/timebase.rs::cycle_observation_from_tick",
            "description": "Causal multiscale observed-ridge timing (issue #92, ADR 0002): one scalar per evidence channel per authoritative hop -> causal constant-Q analytic field -> continuous off-grid frequency/phase -> persistent observed CycleModes + rational-relation diagnostics. The newest-frame extraction from the overlapping feature window lives in Rust only. No global tempo, no beat/bar/phrase trackers, no latent undertones (#97).",
            "surfaces": {
                "rust_authority": "runtime-core/src/cycle_bank.rs",
                "runtime_surface": "wasm-orbit/src/lib.rs (CycleBank wasm_bindgen, observe_tick) consumed via the generated CycleMode/CycleRelation TypeScript interfaces",
                "training_surface": "runtime-core/src/pybindings.rs (CycleBank/CycleMode PyO3) consumed by backend/tests/test_cycle_bank_binding.py"
            },
            "required_tests": [
                "runtime-core/tests/test_cycle_bank.rs",
                "backend/tests/test_cycle_bank_binding.py"
            ]
        }
    },
    "binding_symmetry": {
        "rule": "Every pyclass exported through runtime-core/src/pybindings.rs must have a wasm_bindgen export in wasm-orbit/src/lib.rs, and vice versa, unless exempted below. A canonical subsystem exported to only one consumer surface is the #93 failure mode.",
        "exemptions": [
            "RuntimeVisualMetrics",
            "Complex",
            "CycleMode",
            "CycleRelation",
            "CycleObservation",
            "CycleEvidenceChannel",
            "CycleBankConfig"
        ],
        "exemption_reasons": {
            "RuntimeVisualMetrics": "Training-only diagnostic; the browser computes visual metrics in TypeScript.",
            "Complex": "wasm-only serialization helper, not a canonical subsystem.",
            "CycleMode": "PyO3 read-only view object; the browser consumes the equivalent record via the Rust-authored `CycleMode` TypeScript interface emitted by the wasm custom section. One logical domain type, two surface-idiomatic projections — not a redeclared structural copy.",
            "CycleRelation": "Read-only record reached as data from CycleBank on both surfaces (PyO3 dict / wasm-generated TS interface), not an independent #[wasm_bindgen] struct.",
            "CycleObservation": "Binding-layer input DTO; constructed on each surface and passed into the Rust authority, not a tracked domain state type.",
            "CycleEvidenceChannel": "Binding-layer input DTO, same rationale as CycleObservation.",
            "CycleBankConfig": "Construction-time configuration DTO; the browser runs canonical defaults and does not need a config object."
        }
    }
}